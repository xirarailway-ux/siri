<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Broadcast</title>
  <link rel="stylesheet" href="/public/css/style.css" />
</head>
<body>
  <header class="navbar">
    <div class="brand">TTS Admin</div>
    <nav>
      <a class="<%= nav==='dashboard'?'active':'' %>" href="/admin">Dashboard</a>
      <a class="<%= nav==='users'?'active':'' %>" href="/admin/users">Users</a>
      <a class="<%= nav==='payments'?'active':'' %>" href="/admin/payments">Payments</a>
      <a class="<%= nav==='voices'?'active':'' %>" href="/admin/voices">Voices</a>
      <a class="<%= nav==='plans'?'active':'' %>" href="/admin/plans">Plans</a>
      <a class="<%= nav==='broadcast'?'active':'' %>" href="/admin/broadcast">Broadcast</a>
      <a class="<%= nav==='settings'?'active':'' %>" href="/admin/settings">Settings</a>
      <a href="/admin/logout">Logout</a>
    </nav>
  </header>
  <main class="container">
    <div class="card reveal">
      <div class="header-row">
        <h2 class="section-title">Broadcast</h2>
        <% if (sent) { %><span class="pill pill-green">Sent</span><% } %>
      </div>
      <form method="post" action="/admin/broadcast/send" enctype="multipart/form-data" class="inline" style="gap:12px;flex-wrap:wrap">
        <textarea class="input" name="text" rows="3" placeholder="Message text" style="min-width:280px"></textarea>
        <div class="field"><span class="label">Photo</span><input type="file" name="photo" accept="image/*" /></div>
        <div class="field"><span class="label">Audio</span><input type="file" name="audio" accept="audio/*" /></div>
        <div class="field"><span class="label">Document</span><input type="file" name="document" /></div>
        <div class="actions"><button class="btn btn-primary">Send</button></div>
      </form>
      <span class="note">Caption uses the text if provided. You can send text only, media only, or combinations.</span>
    </div>
  </main>
  <script src="/public/js/ui.js"></script>
</body>
</html>
